PETATOE Mobile Phase P6: reduced production observers and startup timers without changing PETATOE v10.0.25. Removed the diagnostic Runtime Data Trace from the production startup path, eliminating its fetch instrumentation, localStorage trace snapshots, and 50 ms hook polling. Removed the mobile startup gate's document-wide class MutationObserver and retained canonical pointer prefetch plus petatoe:tabchange lazy hydration. Updated the shared mobile runtime coordinator to connect only when subscribers exist and disconnect when unused. Replaced five finance startup retry timers with immediate apply, one bounded fallback, and lazy-group completion events. No business logic, auth, Supabase, API, permissions, service worker, desktop/tablet, localization, or version changes. All syntax, localization, Smart Reports, and Mobile v10 certification checks passed.
