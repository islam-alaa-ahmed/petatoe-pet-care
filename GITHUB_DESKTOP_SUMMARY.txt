PETATOE v10.0.25 hotfix: restored the previously approved Enterprise Glass mobile header and shared movable bottom navigation exactly from the supplied certified modification package. Replaced only mobile/mobile-enterprise-v10-shell.js and css/mobile/mobile-enterprise-v10-consolidated.css, reversing the later mobile shell/CSS presentation regression. Version, business logic, Supabase, database, APIs, authentication, permissions, service worker, desktop/tablet, and localization remain unchanged. JavaScript syntax and CSS structure passed; restored file hashes match the approved package byte-for-byte.
