Mobile UI corrective hotfix on the current baseline without changing the release number.

Fixed:
- Removed leaked legacy search/paw controls below the mobile header.
- Forced the v10 drawer to clear the old sidebar state before opening.
- Isolated and hid the old sidebar/overlay on phones.
- Positioned global search below the safe-area-aware fixed header.
- Stabilized login/MFA layout using visualViewport-aware height and mobile-safe scrolling.

Modified files only:
css/mobile/mobile-enterprise-v10-consolidated.css
mobile/mobile-enterprise-v10-shell.js
security/auth-session.js
