PETATOE v9.4.11 — Permissions Runtime Reference Fix

- Fixed the console-blocking `ReferenceError: userPermStore is not defined` in settings/permissions.js.
- Confirmed the root cause: the Phase 6.1 localized Permissions module exported and used the permission-store APIs after their definitions were omitted.
- Restored the canonical identity-backed `userPermStore()` loader and `saveUserPermStore()` persistence adapter.
- Restored the supporting user-source, escaping, and notification helpers required by the localized Permissions runtime.
- Preserved all existing permission rules and kept the Localization Center integration unchanged.
- Verified that window.PETATOEPermissions loads and exports both permission-store APIs successfully.
- Passed Enterprise Localization Certification and Production Localization Lockdown.
- Passed JavaScript syntax validation for 277 files with zero failures.
- Synchronized release, runtime, index cache tokens, and CI validators to PETATOE v9.4.11.
