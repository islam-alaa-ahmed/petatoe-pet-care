PETATOE Commissions — Employee & Vehicle Identity Resolution Phase 2B.2

- Add one canonical commission identity resolver for vehicles and employees
- Resolve vehicles from explicit IDs, reference/setup/fleet master data, persisted aliases, and deterministic legacy IDs
- Resolve employees from explicit IDs, application users, payroll employees, persisted aliases, and deterministic legacy IDs
- Migrate existing commission employee assignments to stable employeeId and vehicleId values
- Persist identity aliases in the canonical commission reference store
- Group live commission sales and invoice counts by stable vehicle ID instead of mutable display name
- Store personId, employeeId, and vehicleId on calculated commission rows
- Add identity fields to newly locked commission snapshots using commission-snapshot-v2
- Enrich legacy snapshots in memory while preserving their historical display names
- Keep commission formulas, eligibility rules, payroll formulas, Operations, Supabase schema, and UI layout unchanged
- Leave global salesperson business assignment unchanged for a separate mapping phase
