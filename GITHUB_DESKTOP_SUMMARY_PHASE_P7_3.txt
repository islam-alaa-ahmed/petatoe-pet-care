PETATOE v10.0.25 Phase P7.3: introduced an independent mobile dashboard presentation host under the Mobile v10 root. On physical phones the canonical dashboard panel is mounted outside the desktop content tree, while desktop placement is restored exactly on desktop devices. The mobile dashboard retains the shared year filter, YTD banner, payroll shortcuts, KPI data, permissions, and calculations, but prevents creation of the four heavy desktop dashboard Chart.js views and destroys any pre-existing instances. Added stable portrait/landscape KPI layouts without exposing desktop presentation. Updated only dashboard/CSS cache tokens. No version, business logic, Supabase, API, authentication, permissions, service worker, localization dictionary, or desktop dashboard logic changes. Syntax, CSS, localization lockdown, runtime translation, Smart Reports, and Mobile v10 certification passed.
