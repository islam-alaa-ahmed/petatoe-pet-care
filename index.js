/* PETATOE retired root i18n compatibility stub.
   The only live localization DOM engine is i18n/index.js.
   This file intentionally owns no dictionaries, observers, runtime translation APIs,
   or compatibility globals. It remains as a repository tombstone so older references
   fail closed without creating a second localization source. */
(function(){
  'use strict';
})();
