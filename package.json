{
  "name": "petatoe-enterprise-mobile",
  "version": "10.0.0",
  "private": true,
  "description": "PETATOE Enterprise Mobile native wrapper based on Capacitor.",
  "scripts": {
    "native:build:web": "node scripts/build-native-web.mjs",
    "native:sync": "npm run native:build:web && npm run native:install:faceid && npx cap sync ios",
    "native:open:ios": "npm run native:sync && npx cap open ios",
    "native:doctor": "npx cap doctor",
    "native:install:faceid": "node scripts/install-native-face-id.mjs"
  },
  "dependencies": {
    "@capacitor/core": "^7.0.0",
    "@capacitor/ios": "^7.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.0.0",
    "typescript": "^5.7.0"
  }
}
